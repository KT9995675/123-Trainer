# История версий — Математический Тренажер

Формат записи: **версия** → **файл / функция** → **суть** → **статус**

---

## v0.1 (2026-07-04) — базовая версия

| Файл | Содержание | Статус |
|---|---|---|
| `Code.gs` | Серверная логика: вход, выдача задач, проверка ответов, сессия | принято |
| `Index.html` | Интерфейс ученика: вход, тренажёр, экран результата | принято |

Исходники переданы заказчиком. Развёрнуто в Google Таблице.

---

## v0.2 (2026-07-04) — исправление выявленных багов

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `_getTasksByLevel` | Фильтр задач по **первой цифре** ID (ступень), вместо `indexOf`, который давал ложные совпадения | на тесте |
| `Index.html` | *(разметка)* | Убраны тестовые `value` у полей входа; placeholder «ID» вместо «ID или Имя» | на тесте |
| `Index.html` | `handleScriptFailure` | **Новая.** Скрывает лоадер и показывает alert при сбое `google.script.run` | на тесте |
| `Index.html` | `handleLogin` | Подключён `.withFailureHandler(handleScriptFailure)` | на тесте |
| `Index.html` | `loadNextTask` | Отображение «Ступень: N» = `studentLevel + 1` (решаемая ступень); `.withFailureHandler` | на тесте |
| `Index.html` | `handleSendAnswer` | Подключён `.withFailureHandler(handleScriptFailure)` | на тесте |
| `Index.html` | *(восстановление)* | Возвращены блок `quiz-stats` и функция `handleAnswerResponse` из полного исходника заказчика | на тесте |
| `Index.html` | `updateQuizStats` | **Новая.** Обновление счётчиков `quiz-stats` (без дублирования логики `handleAnswerResponse`) | на тесте |

### Чек-лист для тестирования v0.2

1. **Вход** — поля пустые, placeholder «ID»; вход только по ID из колонки A.
2. **Ступень на экране** — ученик с H=0 видит «Ступень: 1»; с H=2 — «Ступень: 3».
3. **Задачи ступени** — на листе «Задачи» задачи `101`, `102`… попадают только в ступень 1; `201`… — в ступень 2.
4. **Сбой сервера** — при отключённом интернете / ошибке GAS лоадер исчезает, показывается alert (не зависает «Загрузка...»).
5. **Основной цикл** — ответ → «Дальше» → новая задача; 7 задач / 3 ошибки — как в v0.1.

---

## v0.3 (2026-07-05) — сессия, лог и визуал прогресса

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `getTaskForUser` | Новая сессия при входе (`startNewSession`); парсинг лога после маркера `##`; отдаёт `correctInSession`, `remainingCorrect` | на тесте |
| `Code.gs` | `submitAnswer` | Победа при **7 верных** (не 7 попыток); провал при 3 ошибках; лог **не стирается** | на тесте |
| `Code.gs` | `_resetSessionCounters` | **Новая.** Обнуляет I, J; добавляет маркер `##` в лог (разделитель сессий) | на тесте |
| `Code.gs` | `_parseCurrentSessionLog` | **Новая.** Читает только текущую сессию (после последнего `##`) | на тесте |
| `Code.gs` | `_buildRunStats` | **Новая.** Единый объект статистики для фронтенда | на тесте |
| `Code.gs` | `_clearSessionProgress` | **Удалена** (заменена на `_resetSessionCounters`) | — |
| `Index.html` | *(разметка)* | Убран блок `quiz-stats`; добавлен ряд из 7 яблочек | на тесте |
| `Index.html` | `renderApples` | **Новая.** 7 яблочек: серые → загораются по числу верных | на тесте |
| `Index.html` | `renderSessionStats` | **Новая.** Обновляет яблочки и сердечки | на тесте |
| `Index.html` | `handleLogin` | Флаг `isFirstTaskAfterLogin` для новой сессии при входе | на тесте |
| `Index.html` | `loadNextTask` | Передаёт `startNewSession` на сервер; убран `stat-progress` | на тесте |
| `Index.html` | `handleSendAnswer` | Обновляет яблочки/сердечки из `correctInSession` | на тесте |

### Чек-лист для тестирования v0.3

1. **Вход** — I=0, J=0; в логе появился маркер `##`; яблочки все серые, 3 сердца.
2. **Верный ответ** — +1 яблочко зажглось; в логе пара `(ID, Да)`.
3. **Неверный ответ** — −1 сердце; в логе `(ID, Нет)`; яблочко не добавилось.
4. **7 верных** — ступень +1, экран «Ура!»; лог **сохранён**, после `##` новая сессия.
5. **3 ошибки** — экран «Упс!»; лог **сохранён**, счётчики обнулены, новый `##`.
6. **Повторный вход** — новая сессия (новый `##`), старый лог на месте; задачи в сессии не повторяются.

---

## v0.4 (2026-07-06) — очередь из 10 задач и prefetch

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `getTaskForUser` | При новой сессии генерирует 10 ID; один раз читает «Задачи»; отдаёт `sessionTasks` на клиент | на тесте |
| `Code.gs` | `submitAnswer` | Проверка по очереди; запись следующего ID в лог при `continue`; пакетная запись H–J | на тесте |
| `Code.gs` | `_createSessionQueue` | **Новая.** 10 случайных ID → CSV после `##` | на тесте |
| `Code.gs` | `_getTasksMapByIds` | **Новая.** Одним проходом по «Задачи» | на тесте |
| `Code.gs` | `_parseCurrentSessionLog` | Читает очередь CSV после `##`; пары начинаются через 2 ячейки | на тесте |
| `Index.html` | `showTaskFromSession` | **Новая.** Показ задачи из локального массива без сервера | на тесте |
| `Index.html` | `prefetchNextImage` | **Новая.** Prefetch картинки следующей задачи | на тесте |
| `Index.html` | `handleNextStep` | «Дальше» без `getTaskForUser` при `continue` | на тесте |
| `Index.html` | `loadNextTask` | Сервер только при новой сессии (вход / win / fail) | на тесте |

### Формат лога сессии (после `##`)

| Ячейка | Содержание |
|---|---|
| `##` | маркер сессии |
| +1 | `101,102,103,...` — очередь из 10 ID |
| +2… | пары `(ID, Да/Нет)` |

### Чек-лист для тестирования v0.4

1. **Вход** — один долгий запрос; дальше «Дальше» **без** паузы 2–3 с.
2. **Prefetch** — картинка следующей задачи появляется быстрее (уже в кеше браузера).
3. **Лог** — после `##` идёт CSV из 10 ID, затем пары.
4. **7 верных / 3 ошибки** — правила v0.3 сохранены.
5. **«Продолжить»** после win/fail — новая сессия с новой десяткой.

---

## v0.5 (2026-07-06) — проверка на клиенте и атомарный лог

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `loginUser` | Возвращает `rowNum` для быстрой записи | на тесте |
| `Code.gs` | `getTaskForUser` | `sessionTasks` с `answer`; ID в лог **не** пишется заранее | на тесте |
| `Code.gs` | `submitAnswer` | Только запись: пара `(ID, Да/Нет)` одним `setValues`; принимает `isCorrect` + `rowNum` | на тесте |
| `Code.gs` | `_parseCurrentSessionLog` | Только **полные** пары; без «висящих» ID | на тесте |
| `Index.html` | `handleSendAnswer` | Проверка на клиенте → мгновенный feedback; «Дальше» после успеха сервера | на тесте |
| `Index.html` | `checkAnswerLocally` | **Новая.** Сравнение ответа локально | на тесте |

### Чек-лист для тестирования v0.5

1. **«Ответить»** — «Верно»/«Ошибка» сразу; кнопка «Сохраняем...»; без белого экрана.
2. **«Дальше»** — появляется только после успешной записи в таблицу.
3. **Лог** — в таблице появляется сразу **две** ячейки: `101` | `Да` (не ID отдельно).
4. **Сбой сети** — feedback сбрасывается, можно ответить снова; в логе нет оборванной пары.
5. **Дальше / win / fail** — как в v0.4.

---

## v0.6 (2026-07-06) — кэш 10 задач сессии на сервере

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `_putSessionCache` / `_getSessionCache` | **Новые.** CacheService: очередь, позиция лога, 10 задач с ответами | на тесте |
| `Code.gs` | `_createSessionQueue` | Возвращает `sessionTasks` (10 задач) для кэша | на тесте |
| `Code.gs` | `getTaskForUser` | Кладёт сессию в кэш; читает задачи из кэша, не из листа | на тесте |
| `Code.gs` | `submitAnswer` | Без разбора всего лога — только кэш + 2 записи в таблицу | на тесте |
| `Code.gs` | `_parseCurrentSessionLog` | Fallback: чтение хвоста строки (32 ячейки) | на тесте |
| `Index.html` | `handleSendAnswer` | Передаёт `sessionIndex` на сервер | на тесте |

### Чек-лист для тестирования v0.6

1. **«Ответить»** — feedback мгновенно; «Сохраняем...» короче, чем в v0.5.
2. **«Дальше»** — по-прежнему мгновенно.
3. После win/fail/нового входа — новый кэш из 10 задач.
4. Лог — пары `(ID, Да/Нет)` атомарно, как в v0.5.

---

## v0.7 (2026-07-06) — запись лога один раз в конце сессии

| Файл | Функция | Что изменено | Статус |
|---|---|---|---|
| `Code.gs` | `finalizeSession` | **Новая.** Запись `##` + очередь + все пары + H/I/J одним блоком при win/fail | на тесте |
| `Code.gs` | `getTaskForUser` | Только чтение уровня и генерация 10 задач; **без записи в лог** | на тесте |
| `Code.gs` | `_buildSessionQueue` | **Новая.** Очередь в памяти (заменяет `_createSessionQueue`) | на тесте |
| `Code.gs` | `_getNextLogColumn` | **Новая.** Поиск следующей колонки лога | на тесте |
| `Code.gs` | `submitAnswer` | **Удалена** (заменена на `finalizeSession`) | — |
| `Code.gs` | CacheService, `_parseCurrentSessionLog` | **Удалены** — сессия целиком на клиенте | — |
| `Index.html` | `handleSendAnswer` | Только клиент: проверка, счётчики, «Дальше» без сервера | на тесте |
| `Index.html` | `saveSessionResult` | **Новая.** Вызов `finalizeSession` при win/fail | на тесте |
| `Index.html` | `onFinalizeError` | До 3 попыток сохранения, потом сессия потеряна | на тесте |
| `Index.html` | `startSessionTimer` | Таймаут 1 ч — незавершённая сессия не пишется в лог | на тесте |

### Политика записи

| Ситуация | Запись в лог |
|---|---|
| Победа / поражение | ✅ Один блок |
| Обрыв, refresh, таймаут 1 ч | ❌ Нет |
| 3 неудачных попытки сохранения | ❌ Сессия потеряна |

### Чек-лист для тестирования v0.7

1. **«Ответить»** — мгновенно, без «Сохраняем...»; «Дальше» сразу.
2. **Середина сессии** — в таблице I,J **не меняются**; в логе нет новых пар.
3. **Победа** — один запрос «Сохраняем результат...»; в логе `##` + очередь + все пары; H+1, I=0, J=0.
4. **Поражение** — аналогично; H без изменений.
5. **Сбой сети при сохранении** — «Повторить сохранение» (до 3 раз); после 3 — выход на логин, лог пустой.
6. **Refresh mid-session** — сессия потеряна, в лог не пишется.
7. **«Продолжить»** после win/fail — новая сессия, вход быстрее (без записи при старте).

---

## v0.8 (2026-07-06) — шапка, горизонтальный макет

| Файл | Что изменено | Статус |
|---|---|---|
| `Index.html` | Двухколоночный макет: слева — шапка «Тренажер "ВСЕМ ПЯТЬ!"», статистика и кнопки; справа — картинка, подсказка, правильный ответ | на тесте |
| `Index.html` | Фиксированная область картинки (`object-fit: contain`); логотип один раз в CSS | на тесте |
| `assets/logo.jpg` | Исходник логотипа | — |
| `Code.gs` | **Не менялся** | — |

### Чек-лист для тестирования v0.8

1. **Горизонтально** (телефон / компьютер) — слева управление, справа условие.
2. **Картинка** — любого размера не выдавливает кнопки.
3. **Подсказка** — по кнопке появляется справа под картинкой.
4. **Ошибка** — слева «Ошибка!», справа блок «Правильный ответ».
5. **Портрет** — колонки складываются вертикально.

---
